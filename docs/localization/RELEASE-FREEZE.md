# Auto Best release verification freeze

The primary localization task is now verifying the combined current source for the owner-requested template release. Do not edit application source, catalog inputs/generated output, configuration, styling, package files or build output concurrently. Documentation-only evidence may still be appended without changing the application.

Retain all owner-requested mobile work, including single-line listing titles, short import labels, drawer headers, subtle selected states and existing sizing tokens. Port 6462 is not stopped or restarted by this task. The primary task owns the 6461 production preview, final build, index and release.

The serial QA runner records before/after source digests and rejects drift. A later source change invalidates earlier exact-source acceptance and requires a rebuilt preview and relevant rerun. Earlier coordination reports and build-2 are historical, not release acceptance.

Current source includes the compact Info/body summaries and footer language-selector wrapper from the latest concurrent correction. Their native catalog/route/interaction checks must run against a fresh build. Business-write blocking remains in the server handler and is independently tested; removing a global presentation notice does not enable delivery.

See HANDOFF.md for the final source/deployment identity and executed results. This freeze is coordination, not a claim that a commit or deployment has occurred.
