# Auto Best

Key: `auto-best` · version: `2026.09.08-refresh-2` · family: `autodeal`.

Polished native SvelteKit derivative. One retained composition; retired variants redirect. Current working files include local polish beyond HEAD.

## Status

source-branded-candidate. The name, logo and icon default to neutral Auto Best branding. Source contact details, social/video content, business copy and sample stock remain as a visual baseline. This is a candidate for personalization, not a finished generic config-driven template or a sendable lead demo.

## Provenance

M:\codex\agency-os-projects\leads\automotive\day-night-auto-group\autodeal-best-day-night; ab92ce9671fb1b56afa38f693755b41aee9c28b0 plus current uncommitted files

Existing license/asset notes remain with the source. No new multi-client rights determination was made. Source instructions and old project task ledgers are historical; J:/cars/AGENTS.md governs this copy.

## Run

Use Node 22.12+ in the Node 22 line. Run `npm ci`.

From J:/cars, on a free port:

```powershell
./scripts/start-preview.ps1 -Template auto-best -Port 6461
```

Suggested library URL: http://127.0.0.1:6461/. The suggestion is not proof that a listener is running; see the audit runtime record. Original inspected source port: 5173.

`npm run validate` (architecture, assets, Svelte check, production build).

## Real homepage choices

- `main`: `/`

Copy the whole project to retain all variants. Select a primary entry after copying; retain alternate-home choice links when requested.

## Personalization boundaries

- `src/lib/config/brand.ts`
- `src/lib/data/company.ts`
- `src/lib/data/inventory.ts`
- `src/app.css`
- `static`

These are current code/data ownership locations, not a promise that one config edits the whole app. Scan every retained route, metadata, contact value and identity-bearing asset after changes.

The 8 September improvement release adds vehicle-aware contact context, retained list state, stock-driven discovery, optional sample sections and modular data/style ownership. Inventory and hero data still belong to the source dealer and must be replaced per client. See the release evidence below for current validation counts.

## Representative QA routes

Current desktop banner sequence: Featured black, body types red, brands black, editorial red, YouTube black, Help red. Owner-approved alternating treatment uses the shared red modifier/token and white banner buttons; content panels remain gray with white cards. Verified on 6461; `alternating-types.png` and `alternating-editorial.png` in the desktop comparison audit.

Latest search clarification: outer search box is white and fields stay light. Only controls carry strong color: ink selected Buy/Import tab and red primary actions. This supersedes the charcoal search casing described below. Verified at 390/1440; screenshots `search-correction-mobile.png` and `search-correction-desktop.png` in the desktop comparison audit.

Latest owner-directed banner/search refinement (8 September): all six desktop sections now share the 216px centered top-banner treatment (ink; Help red), with gray content panels. Body-type artwork uses a 120px image region and 18px labels, capped at its native width or 200px; brand logos use a 72px region. The homepage Buy/Import search box is charcoal on desktop and mobile, with light fields and red selected-tab/actions. This supersedes the quiet-gray-heading direction below. Evidence: Cars `audits/2026-09-08/auto-best-desktop-comparison/BANNER-REFINEMENT.md`.

Desktop composition refinement (8 September): homepage section introductions are centered with a shared 32px heading size. Featured stock precedes body-type discovery in document order. Featured/YouTube retain compact dark top banners; body type, brands, editorial and Help use quiet gray heading areas. Help has filled red service actions, and desktop campaign/service/video arrows use 18px SVGs. Mobile component styling is retained; the stock-first section order also applies there. Evidence: Cars `audits/2026-09-08/auto-best-desktop-comparison/IMPLEMENTATION.md`.

Latest borderless editorial refinement: the three “Полезно” menu images are now photo-derived ImageGen edits on matching gray/red/ink backgrounds, with feathered edges instead of inset rounded photo frames. The unmodified Pexels photographs remain in the blog listing. Titles, menu structure and panel sizing are retained. Provenance: `provenance/borderless-editorial-2026-09-08.md`; evidence: Cars `audits/2026-09-08/auto-best-borderless-editorial/REPORT.md`.

Latest editorial menu correction (8 September): “Полезно” uses three distinct stock photographs for inspection, vehicle transport and a contract discussion, separate from “За нас” service artwork. Its question titles include question marks and match the article records. The corresponding blog-listing photos also match. Card colors, typography and menu geometry are retained. Source credits: `provenance/editorial-photos-2026-09-08.md`; checks: Cars `audits/2026-09-08/auto-best-editorial-photos/REPORT.md`.

Current menu direction (8 September, latest owner clarification): preserve three illustrated cards per mega menu, using solid gray, red or ink surfaces and title + asset only (no card descriptions). Keep the existing menu columns, side links/CTA and shared artwork framing. All desktop disclosures share a 288 px minimum panel and 248 px card height. Compact sidebar links and removal of redundant CTA captions keep switching stable without stretching the cards. The separator beneath navigation is removed and the page backdrop uses 76% opacity. Live text remains separate from imagery. Vehicles and inspection use gray; import uses red; showroom and leasing use ink. Use the new reception, car/container and lease-folder service artwork; retain the original repair image. All four use balanced landscape framing. These are conceptual illustrations, not business photography. Evidence: Cars `audits/2026-09-08/auto-best-service-assets/REPORT.md`.

Earlier menu artwork/CTA follow-up: feature rendering is owned by `NavigationFeatureCard.svelte`; all menus again use the illustrated card composition. `feature-artwork.ts` retains original topic mappings; `ActionLink.svelte` and `--dn-cta-font` own header/menu CTA type, shared with desktop homepage CTA typography. Historical evidence: Cars `audits/2026-09-08/auto-best-header-artwork/REPORT.md`.

Desktop header refinement (8 September): solid ink/red mega-menu cards, category cutouts, quieter 18 px navigation and page scrim; pointer entry into the top information strip dismisses the menu. Evidence: `audits/2026-09-08/auto-best-header/REPORT.md` in the Cars library. This is a follow-up to refresh-2.

- `/`
- `/listing-grid`
- `/listing-detail-v1/1`
- `/about-us`
- `/contact`
- `/blog`

Use 390 and 1440 px, plus every offered home. Exercise navigation, filters, detail return, overlay dismissal and the main contact path. Existing source data and frontend feedback do not prove real form delivery or a working provider integration.

## Source refresh 2026.09.06-refresh-1

Refreshed from the owner-approved current source including uncommitted polish. See J:/cars/audits/2026-09-06/asko96-build/refresh-plan.json and baseline backups. Existing J: README and local runtime configuration are preserved.

## Improvement release 2026.09.08-polish-1

See [ARCHITECTURE.md](ARCHITECTURE.md) for ownership and the [implementation report](J:/cars/audits/2026-09-08/auto-best-improvements/REPORT.md) for findings disposition and current tests. Matched screenshots are in [comparison.html](J:/cars/audits/2026-09-08/auto-best-improvements/comparison.html).

Before browser checks set `$env:BASE_URL='http://127.0.0.1:6461'` after confirming listener ownership. Run `npm run smoke`; `npm run quality` includes browser qualification as well as validation. `template.ts` defaults to preview/noindex, with sample team and partner sections off. Stock is explicitly unverified sample data; publication requires identity and record-level verification. Existing client copies are unchanged.

## Desktop and mobile source refresh 2026.09.08-refresh-2

Updated from a saved snapshot of the current source served on 5173, including its uncommitted presentation changes. A three-way comparison against the pre-improvement baseline transferred 19 source-file changes while preserving the master-specific domain and accessibility work. See the [refresh report](J:/cars/audits/2026-09-08/auto-best-source-refresh/REPORT.md) and `.template/refresh-manifest.json` for source identity, changes and validation evidence.

The refresh adds attached homepage section headers and light content panels, a full-width stock-driven body grid, coordinated campaign spacing, compact video tiles, mobile home/sell/import artwork and updated call/enquiry treatments on contact, detail and article routes. Previously inherited About, inventory and blog compositions remain; the shared shell and brand apply across them. Vehicle context, list/article return state, stock-driven categories, optional sample sections and viewport-specific image requests are retained.

Default identity assets are `static/assets/images/template/auto-best-logo.svg` and `static/auto-best-icon.svg`; the logo is selected in `src/lib/config/brand.ts`, and the icon in `src/app.html`. Replace both during personalization. Historical identity assets remain only for provenance. Existing client copies and the original 5173 project are independent and were not updated.

## Homepage hierarchy 2026.09.09

Desktop discovery now shows eight configured body types (four columns, two rows) and all twelve source brands (six columns, two rows at 1440px). Counts still come from sample inventory; zero-stock categories lead to the real empty results state. Mobile initially shows the four stocked categories/brands and offers accessible expand/collapse buttons. The desktop heading banners use 192px, with compact 176px body/brand banners. Alternating black/red banners and the white search panel are retained.

Homepage guides reuse the canonical article records and images. `body-wagon-v1.png` is generated category artwork created on 2026-09-09 using ImageGen, not stock photography or evidence of a specific vehicle. Earlier artwork remains for provenance. Original source on port 5173 and independent client copies were not modified.

Validation: `npm run validate`; `BASE_URL=http://127.0.0.1:6461 node scripts/home-hierarchy-smoke.mjs`. Captures/results: `artifacts/home-hierarchy/`. This checks 390/1024/1440 layouts, desktop rows, mobile expansion, all 20 discovery links against actual sample counts, overflow and page errors.

2026-09-09 banner follow-up: all six desktop section headers now use title plus CTA only, with a shared 176px height. Body artwork uses a consistent 224px maximum desktop width inside unchanged tiles. Rendered at 390/1440 with no overflow; Svelte check passed with zero warnings/errors.
