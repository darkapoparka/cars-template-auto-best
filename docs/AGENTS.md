# Documentation notes

Use [the index](README.md) to find the document that owns a topic. Explain the current implementation with concrete file names, values and examples. Link to the primary topic instead of repeating it in several guides.

Use UTF-8 Markdown and relative links for repository files. Check commands against `package.json` and distinguish source-version differences explicitly. Routine code changes need an updated relevant paragraph, not another policy document or audit report.
